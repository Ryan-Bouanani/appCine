{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<div class="container text-center"> 
    <h1 class="mb-3">Acteur : {{actor.firstName}} {{actor.lastName}}</h1>  
    <p class="mb-3">{{actor.firstName}} a debuter sa carrière la {{actor.startCarreerAt|date('d/m/Y')}}.</p>  
    <p class="mb-3">Date de naissance : {{actor.dateOfBirth|date('d/m/Y')}}</p>  
    Movies : 
    {% for movie in actor.movies %}
        <p>{{movie.title}}</p>
    {% endfor %}
</div>
<a href="{{ path('app_actor_form', {'idActor':actor.id})}}" class="btn btn-primary">Ajouter un actor</a>
<a href="{{ path('app_home')}}" class="btn btn-primary">Retour</a>

{% endblock %}